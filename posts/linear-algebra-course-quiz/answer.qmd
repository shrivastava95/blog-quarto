---
title: "Answer Key: Linear Algebra Course Quiz"
format:
  html:
    toc: true
    number-sections: true
---

# Answers

1. \(\mathbf r(t)=(1,2)+t(3,-1)=(1+3t,2-t)\).
2. It contains \(0\), and if \(A\mathbf u=0\) and \(A\mathbf v=0\), then \(A(a\mathbf u+b\mathbf v)=aA\mathbf u+bA\mathbf v=0\).
3. The inner product is \(2+0-3=-1\); the norms are \(\sqrt6\) and \(\sqrt{13}\).
4. \(\boxed{(2,-1,-5)}\).
5. \(\boxed{\begin{bmatrix}2&-1\\1&3\end{bmatrix}}\).
6. If \(A=[\mathbf a_1\ \cdots\ \mathbf a_n]\), then \(A\mathbf x=\sum_i x_i\mathbf a_i\), so the columns encode the images of the standard basis vectors.
7. No: \((2,4)=2(1,2)\).
8. Solve \(a(1,1)+b(2,-1)=(5,1)\), giving \(\boxed{a=\frac73,b=\frac43}\).
9. \(\boxed{\begin{bmatrix}1&-1\\-1&2\end{bmatrix}}\).
10. \(\dim(V)=\operatorname{rank}(A)+\operatorname{nullity}(A)\); dimensions split into independent output directions and input directions sent to zero.
11. Free variables are parameters; pivot variables are expressed in terms of them, producing infinitely many solutions when at least one free variable remains.
12. \(\boxed{10}\).
13. A square matrix is invertible iff \(\det(A)\ne0\).
14. \(p_A(\lambda)=(3-\lambda)(2-\lambda)\).
15. For \(\lambda=3\), \(E_3=\operatorname{span}\{(1,0)\}\); for \(\lambda=2\), \(E_2=\operatorname{span}\{(-1,1)\}\).
16. A linear dependence among eigenvectors with distinct eigenvalues, acted on by \(A\), produces a second relation with distinct scalar factors; subtracting forces all coefficients to vanish.

# Grading

Award 2 points for a correct and explicit answer, 1 for the correct method with a minor error, and 0 for a missing or materially incorrect answer.
